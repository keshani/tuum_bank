spring:
  datasource:
    ##url: jdbc:postgresql://postgresqldb:5432/db_tuum
    url: jdbc:postgresql://postgresql:5432/db_tuum
    username: postgres
    password: psw_tuum
    driverClassName: org.postgresql.Driver

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

server:
  port: 9093

mybatis:
  configuration:
    use-generated-keys: true
